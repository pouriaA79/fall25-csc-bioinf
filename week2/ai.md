### AI TOOL AND MODEL
Gemini — 2.5 Flash
------------------------------------
### WHAT I USED AI FOR
1. Explaining Codon type-realization errors and adding explicit type hints to classes.
2. Resolving `matplotlib` errors related to large image dimensions (`Image size too large`).
3. Generating a robust and standalone test suite (`test.py`) covering all major components of the project.

-------------------
### PROMPTS

1.  "What are the correct type annotations for all variables and function return values in my Codon script?"
2.  "How can I resolve the `PyError: Image size of ... pixels is too large` in Matplotlib when plotting a large dataset?"
3.  "Write a standalone test script for my project that verifies the functionality of the `Decomposer`, `Encoder`, and `MotifAligner` classes in a Codon-compatible way."
